version: '3.1'
services:
  postgresql:
    image: library/postgres:12-alpine
    ports:
      - '9999:9999'
    volumes:
      - ./data:/var/lib/postgresql
    environment:
      - POSTGRES_PASSWORD=9mREsvXDs9Gk89Ef
      - POSTGRES_USER=fimena